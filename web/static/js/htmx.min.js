/*! Minimal htmx stub for offline dashboard (not full feature complete). */
(function(){
  if (window.htmx) return;
  window.htmx = {
    version: "0.0.1-local",
    find: function(){ return []; },
    on: function(){},
    trigger: function(){},
  };
})();
